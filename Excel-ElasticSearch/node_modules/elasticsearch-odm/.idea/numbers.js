var utils = require('../lib/utils');

var long = 9223372036854775807;
var short = -32768;
var byte = -127;
var double = 32523.352;
var base64 = 'aGhlbGxv';
var isoDate = new Date().toISOString();

console.log(   utils.isLong(long)  )
console.log(   utils.isShort(short)  )
console.log(   utils.isByte(byte)  )
console.log(   utils.isFloat(double)  )
console.log(   utils.isBase64(base64)  )
console.log(   utils.isISO8601(isoDate)  )
